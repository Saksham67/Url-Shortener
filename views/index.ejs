<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Url Shortener</title>
    <style>
      body{
        margin: 0;
        padding: 0;
        background-color: #3C467B ;
        font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        text-align: center;

      }
      .container{
        background-color: #696FC7;
        padding: 25px;
        border-radius: 25px;

      }
      form{
        padding: 25px;
  
        font-size: 28px;
        /* text-align: center; */


      }
      input{
            padding: 15px;
            margin: 8px;
            border: #31326F;
            border-radius: 35px;
            font-size: 18px;
        }
      button{
            font-size: 20px;
            padding: 18px;
            margin: 15px 15px;
            border: #31326F;
            border-radius: 18px ;
            cursor: pointer;
            color: rgb(118, 156, 237);
            background-color: #19183B;
        }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Url Shortener</h1>
      <form action="shorted" method="post">
        <label for="longUrl">Enter long URL</label>
        <input type="text" id="longUrl" name="longUrl" required /> <br>
        <button type="submit">Short</button>

        <% if(shortUrl) { %>
        <p>
          Shorted URL: <a href="<%=shortUrl %>" target="_blank" /> <%= shortUrl
          %>
        </p>
        <% } %>
      </form>
    </div>
  </body>
</html>
